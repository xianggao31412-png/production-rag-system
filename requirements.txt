# Core runtime dependencies (light — the platform boots with only these).
# Versions are lower-bound pinned to what the project was tested against.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
numpy>=1.26
pypdf>=4.2
python-multipart>=0.0.9
rank-bm25>=0.2.2
PyYAML>=6.0
python-dotenv>=1.0
